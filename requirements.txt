# requirements.txt — Dependencies for AI-Powered Real-Time IDS

# ── Core ML ──
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0
imbalanced-learn>=0.11.0
xgboost>=2.0.0

# ── Visualization ──
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0

# ── Dashboard ──
streamlit>=1.30.0
openpyxl>=3.1.0

# ── Real-Time Network Monitoring ──
psutil>=5.9.0

# ── Packet Capture (optional — for full Scapy live mode) ──
# scapy>=2.5.0
# NOTE: Scapy also requires Npcap on Windows (https://npcap.com)
#       psutil mode works without Scapy or Npcap.

# ── Utilities ──
# urllib3 (built-in, no install needed)
